<UserControl x:Class="Geosys_Plugin.Views.Plugin.ErrorPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Geosys_Plugin.Views.Plugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{Binding Path=ui.ErrorPanelViewModel}">
    <Grid  Margin="10,30,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock  Grid.Column="0" Grid.Row="0" Text="{Binding ErrorMessage}" TextWrapping="WrapWithOverflow" Width="360"></TextBlock>
    </Grid>
</UserControl>
